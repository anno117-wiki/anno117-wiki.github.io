{
  "name": "anno117-calculator-tools",
  "version": "1.0.0",
  "description": "Tools for Anno 117 Calculator",
  "scripts": {
    "dev": "bun run devserver.ts --dev",
    "build": "bun run devserver.ts --build",
    "commit": "bun run devserver.ts --build --commit",
    "list": "bun run devserver.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.5.0"
  },
  "private": true,
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "pre-commit": "^1.2.2"
  }
}
